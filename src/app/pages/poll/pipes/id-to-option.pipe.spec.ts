import { IdToOptionPipe } from './id-to-option.pipe';

describe('IdToOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new IdToOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
